# T64 — Bullet-time in the run loop

**Branch** `task/T64-run-slowmo` · **PR title** `T64: Left Shift slows the run loop`

The combat spine's second half enters the main loop: `RunSlowMo` ports the
owner-approved CombatSpike feel (Left Shift, T48 `SlowMoResource` revolver
charge 2.5s drain / 8s recharge / 0.3 min-engage, `TimeDilationService`
global dilation, camera on unscaled time) into the generated-floor run.
Deliberately excluded: the spike's `CounterWindow`/`SetPillbug` coupling —
counters stay owner-frozen. HUD gains a thin amber charge gauge under the
run panel.

## Verification

- HUD gauge pass-through test; full EditMode **502/502**.
- FloorEncounter Windows64 Succeeded 0/0; desktop hold-Shift capture shows
  dilation + gauge drain, release/depletion restores; 0 exceptions.
