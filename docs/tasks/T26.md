# T26 — EncounterBudget (조우 예산)

/ 근거: vault `40 §6`, `36 Reference §6`, `34 §9`. 현재 조우 생성(T7/T13, `clamp(1+depth+sizeBonus,1,5)`)은 초기형. 난이도를 단일 숫자가 아니라 **예산 객체**로 분리한다: 동시 활성 적 수 상한, 적 종류 수, 웨이브 수, 엘리트 수, 총 난이도 예산이 각각 depth로 램프.

## v0 규칙 (바꾸지 말 것)
1. **Core 순수·결정적.** 시드 기반, RNG 노출 금지. Physics/Time.timeScale 금지. manifest 무변경.
2. 기존 `FloorGenerator`/`FloorEncounter`/`FloorEnemySlot`/`IExpeditionEnemyFactory`(Gen)와 시뮬(`AutoBattleSimulator`)의 결정성 계약 유지.

## 산출물
1. **Core 데이터** `EncounterBudget`: baseDifficulty, depthDifficultyRamp, activeEnemyCapBase, activeEnemyCapMax, activeEnemyCapDepthRamp, enemyTypeMin/Max(+typeCountDepthRamp), waveMin/Max, eliteCap, (옵션)manualWaveTemplates. 바이옴/방종류별 override 가능한 구조.
2. **예산→조우 컴포저**(Gen): 예산 + 방깊이 + 시드 → 결정적으로 (적 종류 선택, 마리수, 웨이브 분할, 엘리트 배정)을 산출해 `FloorEncounter`/`FloorEnemySlot`을 채운다. 기존 flat 공식을 대체하되 v0 기본값은 현재 밸런스와 크게 어긋나지 않게(입구 무조우·보스층 규칙 유지).
3. **QA/계측 연동**: 조우별 예산 파라미터가 계측(CombatMetrics/AutoBattle)에서 확인 가능하도록(선택).
4. **EditMode 테스트**: 같은 시드=같은 조우, depth 램프(깊을수록 예산↑), 활성 상한·엘리트 상한 준수, 종류/웨이브 범위 준수. 기존 테스트 유지(조우 수치 변경으로 깨지는 기존 테스트는 브리프 기준으로 갱신하고 PR 본문에 명시).

## 완료 정의
- 배치모드 EditMode `result="Passed"`(전체 + 신규), 0 error. 시뮬 결정성(같은 시드=같은 집계) 유지.
- 브랜치 `task/T26`, main 직push 금지. v0 결정·테스트 갱신 내역 보고.

## 함정 (Windows 호스트)
- `git -C C:\Users\fancy\Tower`. gpgsign 실패 시 `-c commit.gpgsign=false`. 산출 .cs만 커밋(.meta 제외 — 오케스트레이터 생성). Unity 실행 금지. 최신 main에서 분기.
