# T29 — DE 상호작용 앵커 (풀 세트)

/ 근거: vault `33 Reference §4·§6·§7`, `40 §3`. "방이 비어 보이는" 문제 = 앵커 밀도 부재. DE 질감 = "이 장소에 내가 읽을 것이 있다"는 밀도. 오브=대사가 아니라 **인지 표식**(회귀자 기억/동료 경고/탑의 감각). 문/오브/컨테이너/묘비/함정을 공통 상호작용 상태로.

## v0 규칙 (바꾸지 말 것)
1. **Core 순수.** Physics/Time.timeScale 금지. manifest 무변경. 결정성·QA state 계약 유지.
2. **T25 PortalDef 재사용**: 문 앵커는 T25의 PortalDef/PortalOffer를 표시원으로 쓴다(중복 구현 금지). T25가 main에 있어야 함(선행).
3. T22 주스 프레젠터·T23 리본 불간섭. 대사량 늘리지 말 것(텍스트 생산 아님, 앵커 affordance).

## 산출물
1. **Core 데이터**: `InteractableDef`(id, kind: portal|chest|shrine|grave|trap|inspect|resource, prompt, disabledReason, visibilityRule, useRule, riskPreview, rewardPreview, requiredTags[], stateChanges) + `InteractionState`(visible, enabled, disabledReason, preview, usesRemaining, recordsOnUse) + `OrbCueDef`(id, cue kind: memory|companion|hazard|loot, 조건: 스킬/성향/회귀 기억, 짧은 표식 텍스트).
2. **상호작용 상태 엔진**(Core, 순수): 방/캠프 상태 + party/floor/retreat/death/biome로 각 앵커의 visible/enabled/disabledReason/preview를 결정. 잠금은 무응답이 아니라 "왜 지금 안 되는지" 표시.
3. **Expedition/Camp 배선**: 방에 앵커 배치 — 문(=PortalDef), 기억 오브(orb_memory), 동료 오브(orb_companion), 컨테이너, 위험(hazard), 묘비(캠프, 확정사망/실종). hover 상태 + disabled 사유 표시. 3D 앵커 우선(33 §6·§8: 랜덤 팝업/`Next` 버튼 금지).
4. **QA state**: hover 가능 앵커 목록, locked reason, selected interaction id, orb 필터(기본/인지/위험/전리품).
5. **EditMode 테스트**: 순수 상호작용 상태 로직(eligibility, disabledReason, use 기록, orb cue 조건 매칭). 기존 테스트 유지.

## 완료 정의
- 배치모드 EditMode `result="Passed"`(전체 + 신규), 0 error. 결정성/QA 불변.
- 브랜치 `task/T29`, main 직push 금지. v0 결정 보고.

## 함정 (Windows 호스트)
- `git -C C:\Users\fancy\Tower`. gpgsign 실패 시 `-c commit.gpgsign=false`. 산출 .cs만 커밋(.meta 제외). Unity 실행 금지. **T25 머지된 최신 main에서 분기.**
