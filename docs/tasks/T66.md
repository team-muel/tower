# T66 — Defeat, retreat and conquest read on screen

**Branch** `task/T66-run-transitions` · **PR title** `T66: the run's turning points fade in`

`RunTransitionPresenter` (unscaled-time IMGUI overlay: fade in / hold / fade
out) gives the three lifecycle turning points a face: 후퇴 (cause + count/3),
대회귀 (third retreat), 층계 정복 (events + platinum). The T58 defeat handler
refactors into a shared `RetreatRun(cause)` also used by the new **voluntary
retreat** — hold R for 1.2 s outside active combat (미결 후퇴 경제의 입력
전제). Final styling belongs to the art pass; T65 numbering stays reserved
for the ability-input lane (owner boundary pending).

## Verification

- Presenter fade-curve test; full EditMode **503/503**.
- FloorEncounter Windows64 Succeeded 0/0; desktop R-hold retreat shows the
  후퇴 overlay and returns to floor one; 0 exceptions.
