# T13 — 인게임 QA 자동화 도구 도입

브랜치: `task/T13-qa-harness` / PR 제목: `T13: in-game QA harness`
의존: T10 (머지됨). 배경: Win32 클릭 주입 QA는 기각(사용자 결정, 2026-07-06) — 게임 상태를 구조적으로 조회·조작할 채널 필요.

## 1단계: 평가 (PR 본문에 결과 기록)

후보를 30분 내로 조사·판정 (예산 0원 제약):
- **AltTester Unity SDK**: 라이선스(무료 티어 존재 여부 2026 기준)·Unity 6 호환 확인. 무료면 1순위.
- **Airtest+Poco**: Unity SDK 통합 비용 확인.
- **GameDriver**: 상용 — 무료 티어 없으면 기각.
- Unity AssistiveSupport API: 접근성(스크린리더)용 — 자동화 채널로 부적합 예상, 짧게 확인만.

## 2단계: v0 구현 — 어느 쪽이든 다음을 충족

**dev 빌드 전용 QA 채널** (`-qaPort <n>` 커맨드라인 인자 있을 때만 활성, 릴리스 빌드 제외):
1. TCP 라인 프로토콜 (localhost): `press <buttonName>` (uGUI Button을 이름으로 onClick invoke), `state` (현재 씬·이니셔티브 큐·파티 HP·원정 상태를 JSON 덤프), `scene <name>` (씬 로드), `quit`.
2. 외부 도구(AltTester 등)를 채택했다면 그 SDK 통합 + 위 4개 동작의 사용 예시 스크립트(PowerShell)를 docs/CONVENTIONS.md에 추가.
3. 자체 하니스라면 Tower.Combat/UI에 리플렉션 없는 명시적 레지스트리(QA에 노출할 버튼·상태 공급자 등록) — 하드코딩 분기 금지 원칙 유지.
4. EditMode 테스트: 명령 파서, 상태 덤프 직렬화. + 실동작 검증: dev 빌드 1회 뽑아 TCP로 press/state 왕복 성공 로그를 PR에.

## 규약 리마인드

git -C C:\Users\fancy\Tower, gpgsign=false, main 직push 금지, PC_RPAsset/Packages 커밋 금지. 배치모드 env(ALLUSERSPROFILE 등) 후 테스트 전체 통과.
