# T25 — PortalDef + 문 보상 미리보기 (Hades 경제)

/ 근거: vault `40 §4`, `34 Reference §6·§10`, `36 §10·§11`. 핵심: **문은 누른 뒤 결정되는 게 아니라, 열린 시점에 이미 다음 방+보상이 걸려 있다.** "선택 전에 트레이드오프가 보이는 것"이 런 경제의 맛. T17의 `nextRoomPreview`(UI state)를 Core 데이터 `PortalDef`로 승격한다.

## v0 규칙 (바꾸지 말 것)
1. **Core 순수.** Physics/Time.timeScale 금지. 결정성 유지(시드 기반, RNG 노출 금지). manifest 무변경.
2. 기존 `FloorGenerator`/`FloorRoom`/`FloorDoor`/`FloorEncounter`(Tower.Gen)와 T17 문 preview 위에 얹는다. 기존 계약 깨지 말 것.
3. 보상/방 후보는 **문이 열리는 시점(전투 클리어 또는 방 진입)에 확정**되어 이후 표시. 클릭 시 새로 뽑지 않는다.

## 산출물
1. **Core 데이터**: `PortalDef`(fromRoomId, toRoomCandidate=roomKind/biome/depth, rewardCandidate=type+magnitude, riskTags[], lockReason(nullable), rerollAllowed(bool), entranceDir/exitDir) + `RewardType` enum(회복/자원/능력/숏컷 등 v0 소수) + 경량 `PortalOffer` 리스트(방별 제시 포털).
2. **결정적 배정기**(Gen 또는 Core): 시드+방깊이+바이옴 기반으로 각 문에 toRoomCandidate+rewardCandidate+risk 배정. Hades 우선순위 개념 축소: ForceNext > Linked > EligiblePool. 반복 억제(같은 보상 연속 억제) v0.
3. **Expedition 배선**(PlayableExpeditionController): 방 클리어 시 문에 PortalOffer 부착, UI에 보상 타입+위험 미리보기 표시(T17 preview를 PortalDef 기반으로). 3D 문 앵커/포털 클릭 흐름 유지.
4. **QA state**: `offeredPortals`(각 포털의 reward type, risk tags, locked reason, reroll 여부).
5. **EditMode 테스트**: 배정 결정성(같은 시드=같은 offer), lockReason 규칙, reroll 정책, 반복 억제. 기존 360 유지.

## 완료 정의
- 배치모드 EditMode `result="Passed"`(≥360 + 신규), 0 error. 결정성/시뮬 테스트 불변(변경 불가피하면 PR 본문에 근거+테스트 갱신 명시).
- 브랜치 `task/T25`, main 직push 금지. PR 불필요(오케스트레이터 게이트/머지). v0 결정은 커밋 메시지/보고에 명시.

## 함정 (Windows 호스트)
- `git -C C:\Users\fancy\Tower`. gpgsign 실패 시 `-c commit.gpgsign=false`. 산출 .cs만 커밋(.meta·*.unity·PC_RPAsset.asset·Packages 제외 — 오케스트레이터가 .meta 생성). Unity 실행 금지(게이트는 오케스트레이터). 최신 main에서 분기.
