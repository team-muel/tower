# T61 — Meta progression spine

**Branch** `task/T61-meta-progress` · **PR title** `T61: platinum pierces the regression`

## Contract

- `MetaProgress` (Core-pure): platinum balance, conquest count, shortcut
  stairway record (재등반 압축 seam), and per-faction slot unlocks. Persisted
  in its own file (`meta-progress.json`, `MetaProgressRepository`) apart from
  the run save, so it pierces retreat, the great regression, and
  `-qaFreshRun` — the 2026-07-08 canon for 백금화.
- Conquering the stair-step records the shortcut and grants platinum
  (`PlatinumPerConquest = 1`, provisional — economy detail stays owner-frozen;
  all prices are deterministic placeholders as data).
- **Slot unlock is the one bonus wired end-to-end** (`SlotUnlockCost = 3`),
  matching the owner-confirmed lever (2026-07-08: 증가=백금화 해금, 시작 2 /
  최소 1 / 최대 4). `SlotCountFor` feeds the encounter host through
  `SetPlayerSlotOverride`, clamped to loadout bounds. Stat/cooldown/HP
  bonuses (13 §7) remain named seams for their own combat-wiring lane.
- 직군 매핑 = `CharacterDef.FactionId` 1:1 (최소구현 canon).
- HUD shows the platinum balance on the reward panel.

## Explicitly out of scope

- 영입/편성 UI and roster composition changes (needs owner input on the
  roster loop), stat/cooldown/HP bonus combat wiring, real economy values,
  multi-stairway shortcut traversal (single stairway exists today).

## Verification

- MetaProgressTests: **5/5 passed** (grant/shortcut, pricing/caps, slot
  bounds, repository round-trip, corrupt-snapshot rejection).
- Full EditMode: **499/499 passed**, 0 failed, 0 skipped.
- FloorEncounter Windows64: **Succeeded**, Errors=0, Warnings=0.
- Desktop: fresh run + encounter victory with the meta spine active,
  0 runtime exceptions. The desktop `[MetaProgress] Conquest recorded`
  proof lands with the T62 full-run harness (conquest takes a whole run).
