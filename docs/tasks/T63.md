# T63 — Camera canon unification

**Branch** `task/T63-camera-canon` · **PR title** `T63: the owner-tuned orbit camera drives the run loop`

Owner feedback 2026-07-14: the camera tuned in the Prototype was not reused in
the run-loop build. Fix: `IsoCameraFollow` (2026-07-12 owner values —
basePitch 25, yawSensitivity 4, pitchSensitivity 1.6, distance 14, ±10 pitch
spring-back, scroll zoom) moves into `Tower.Floor` (same GUID, Prototype scene
reference intact) and replaces the rotation-less `ForestFloorCamera` fallback.
`ForestPlayerController` was already camera-relative, so orbiting steers
naturally. Occlusion fade (T60) is target-based and camera-agnostic.

## Verification

- New orbit-canon test + full EditMode: see PR (**501/501**).
- FloorEncounter Windows64 Succeeded 0/0; desktop drag-orbit sanity via capture.
